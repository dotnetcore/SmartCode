Module: DvdRental
Author: Beginor
DataSource:
  Name: DbTable
  Paramters:
    DbName: test_db
    DbProvider: PostgreSql
    ConnectionString: server=127.0.0.1;port=5432;database=dvd_rental;user id=postgres;password=1a2b3c4D;
    DbSchema: public
Language: CSharp
TemplateEngine: Razor
Output:
  Type: File
  Path: '/Users/zhang/Projects/smart-code-sample-project'
  Mode: Full

Build:

  Entities:
    Type: Table
    Module: Data
    IncludeTables: ["actor", "address", "category", "city", "country", "customer", "film", "film_actor", "film_category", "inventory", "language", "payment", "rental", "staff", "store"]
    IgnoreTables: ["film_actor", "film_category"]
    Template: NHibernate/Entity.cshtml
    Output:
      Path: '{{Project.Module}}.{{Build.Module}}/Entities'
      Name: '{{Items.CurrentTable.ConvertedName}}'
      Extension: '.cs'
    NamingConverter:
      Table:
        Tokenizer:
          Type: Default
          Paramters:
            IgnorePrefix: 'T_'
            Delimiter: '_'
        Converter:
          Type: Pascal
          Paramters: { }
      View:
        Tokenizer:
          Type: Default
          Paramters:
            IgnorePrefix: 'V_'
            Delimiter: '_'
        Converter:
          Type: Pascal
      Column:
        Tokenizer:
          Type: Default
          Paramters:
            Delimiter: '_'
        Converter:
          Type: Pascal
  Hbm:
    Type: Table
    Module: Data
    IncludeTables: ["actor", "address", "category", "city", "country", "customer", "film", "film_actor", "film_category", "inventory", "language", "payment", "rental", "staff", "store"]
    IgnoreTables: ["film_actor", "film_category"]
    Template: NHibernate/Hbm.cshtml
    Output:
      Path: '{{Project.Module}}.{{Build.Module}}/Entities'
      Name: '{{Items.CurrentTable.ConvertedName}}'
      Extension: '.hbm.xml'
    NamingConverter:
      Table:
        Tokenizer:
          Type: Default
          Paramters:
            IgnorePrefix: 'T_'
            Delimiter: '_'
        Converter:
          Type: Pascal
          Paramters: { }
      View:
        Tokenizer:
          Type: Default
          Paramters:
            IgnorePrefix: 'V_'
            Delimiter: '_'
        Converter:
          Type: Pascal
      Column:
        Tokenizer:
          Type: Default
          Paramters:
            Delimiter: '_'
        Converter:
          Type: Pascal
